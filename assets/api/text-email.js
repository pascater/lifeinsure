export const textEmailRichiediOfertIT = (idOferta) =>
  `Testa email per richiedere un'offerta per l'id ${idOferta}`;

export const textEmailRichiediOfertFR = (idOferta) =>
  `Texte de l'email pour demander une offre pour l'id ${idOferta}`;

export const textEmailRichiediOfertDE = (idOferta) =>
  `E-Mail-Text zur Anfrage eines Angebots für die ID ${idOferta}`;

export const textEmailEmissioneIT = (idOferta) =>
  `Testa email per richiedere l'emissione dell'offerta per l'id ${idOferta}`;

export const textEmailEmissioneFR = (idOferta) =>
  `Texte de l'email pour demander l'émission de l'offre pour l'id ${idOferta}`;

export const textEmailEmissioneDE = (idOferta) =>
  `E-Mail-Text zur Anfrage der Ausstellung des Angebots für die ID ${idOferta}`;

window.textEmailEmissioneIT = textEmailEmissioneIT;
window.textEmailEmissioneFR = textEmailEmissioneFR;
window.textEmailEmissioneDE = textEmailEmissioneDE;
